export default defineBackground(() => {
  // Milestone 3: AnkiConnect client lives here — calls must originate from the
  // extension origin (content-script fetches carry the page origin, which
  // AnkiConnect rejects).
});
